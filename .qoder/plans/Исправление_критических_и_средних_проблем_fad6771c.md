# Исправление критических и средних проблем

## Проблема BUG-002: Отсутствие обработки ошибок при удалении документов реализации
**Файл:** `backend/app/routers/documents_realization.py` (строки 35-55)

**Текущий код:**
```python
@router.delete("/{doc_id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_realization_document(doc_id: int, db: Session = Depends(get_db)):
    doc = db.query(RealizationDocument).filter(RealizationDocument.id == doc_id).first()
    if not doc:
        raise HTTPException(status_code=404, detail="Document not found")
    
    # Возвращаем товары на склад
    for item in doc.items:
        product = db.query(Product).filter(Product.id == item.product_id).first()
        if product:
            product.total_quantity += item.quantity
    
    # Удаляем бронь
    db.query(Reservation).filter(
        Reservation.reservation_id == doc.reservation_id
    ).delete()
    
    # Удаляем документ
    db.delete(doc)
    db.commit()
    return
```

**Исправление:**
Добавить try/except block с rollback транзакции:
```python
@router.delete("/{doc_id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_realization_document(doc_id: int, db: Session = Depends(get_db)):
    doc = db.query(RealizationDocument).filter(RealizationDocument.id == doc_id).first()
    if not doc:
        raise HTTPException(status_code=404, detail="Document not found")
    
    try:
        # Возвращаем товары на склад
        for item in doc.items:
            product = db.query(Product).filter(Product.id == item.product_id).first()
            if product:
                product.total_quantity += item.quantity
        
        # Удаляем бронь
        db.query(Reservation).filter(
            Reservation.reservation_id == doc.reservation_id
        ).delete()
        
        # Удаляем документ
        db.delete(doc)
        db.commit()
    except Exception as e:
        db.rollback()
        raise HTTPException(status_code=500, detail=f"Ошибка удаления: {str(e)}")
    return
```

---

## Проблема BUG-003: Race Condition при бронировании
**Файл:** `backend/app/routers/admin_reservations.py` (строка 124)

**Текущий код:**
Проверка пересечений и создание брони не атомарны.

**Исправление:**
Добавить блокировку на уровне БД с использованием `with_for_update()`:
```python
# В функции check_overlap добавить FOR UPDATE
overlap = db.query(models.Reservation).filter(
    models.Reservation.bath_id == bath_id,
    models.Reservation.start_datetime < end_dt,
    models.Reservation.end_datetime > start_dt
).with_for_update().first()
```

---

## Проблема BUG-006: Небезопасное хранение токена
**Файлы:** 
- `frontend/my-banya/src/App.js` (строка 49)
- `frontend/my-banya/src/pages/Admin/Admin.jsx` (строка 29)

**Исправление:**
Перейти на использование httpOnly cookies. Требуется:
1. Настроить backend на отправку токена в cookie
2. Изменить frontend на работу с cookie вместо localStorage
3. Обновить API client для отправки credentials

---

## Проблема BUG-008: Не обновляются данные после изменений
**Файл:** `frontend/my-banya/src/pages/Admin/Reservations/BookingDetailsModal.jsx`

**Исправление:**
Добавить cache invalidation после обновления статуса:
```javascript
await updateReservation({...}).unwrap();
dispatch(reservationApiSlice.util.invalidateTags(['Reservation']));
```

---

## Проблема BUG-009: Hardcoded значения времени уборки
**Файл:** `frontend/my-banya/src/pages/Admin/Reservations/AdminReservations.jsx` (строки 29-30)

**Текущий код:**
```javascript
const cleaningMinutes = 30;
const bookingInterval = 30;
```

**Исправление:**
Получать из API настроек:
```javascript
const { data: settings } = useGetSettingsQuery();
const cleaningMinutes = settings?.cleaning_time_minutes || 30;
const bookingInterval = settings?.booking_interval_minutes || 30;
```

---

## Проблема BUG-010: Не обновляются данные после изменений
**Файл:** `frontend/my-banya/src/components/Admin/MobileBottomNav.jsx`

**Исправление:**
Добавить проверку прав доступа для каждого пункта меню:
```javascript
const { user } = useSelector(state => state.auth);

const hasAccess = (permission) => {
  if (!user || !user.permissions) return false;
  return user.permissions.includes(permission) || user.is_admin;
};

const navItems = [
  hasAccess('reservations:view') && { path: '/admin/reservations', ... },
  hasAccess('storage:view') && { path: '/admin/storage/nomenclature', ... },
  // ...
].filter(Boolean);
```

---

## Проблема BUG-016: CORS позволяет все источники
**Файл:** `backend/app/main.py`

**Текущий код:**
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=['*'],
    ...
)
```

**Исправление:**
```python
ALLOWED_ORIGINS = os.getenv("ALLOWED_ORIGINS", "http://localhost:3000").split(",")
app.add_middleware(
    CORSMiddleware,
    allow_origins=ALLOWED_ORIGINS,
    ...
)
```

---

## Проблема BUG-017: Отсутствие Rate Limiting
**Файл:** `backend/app/main.py`

**Исправление:**
Добавить middleware для ограничения запросов:
1. Установить `slowapi`
2. Настроить limiter на 100 запросов/минуту
3. Применить к endpoint'ам авторизации строже (10/минуту)

---

## Проблема BUG-020: N+1 Query Problem
**Файл:** `backend/app/routers/admin_reservations.py` (строки 75-87)

**Текущий код:**
```python
for res in reservations:
    res.products = [
        ...
        for rp in res.reservation_products
        if rp.product is not None
    ]
```

**Исправление:**
Использовать `joinedload`:
```python
from sqlalchemy.orm import joinedload

reservations = query.options(
    joinedload(models.Reservation.reservation_products)
    .joinedload(models.ReservationProduct.product),
    joinedload(models.Reservation.status_rel)
).all()
```

---

## Проблема BUG-022: Загрузка всех данных без пагинации
**Файлы:** 
- `backend/app/routers/products.py`
- `frontend/my-banya/src/pages/Admin/Storage/Storage.jsx`

**Исправление:**
Добавить пагинацию на backend:
```python
@router.get("/products")
def get_products(skip: int = 0, limit: int = 50, db: Session = Depends(get_db)):
    products = db.query(models.Product).offset(skip).limit(limit).all()
    total = db.query(models.Product).count()
    return {"items": products, "total": total, "skip": skip, "limit": limit}
```

И пагинацию на frontend с использованием react-query infinite scroll.

---

## Проблема BUG-004: Неиспользуемые компоненты
**Файлы:** 
- `frontend/my-banya/src/App.js` (строки 11, 18)
- `frontend/my-banya/src/pages/Kitchen/Kitchen.jsx`
- `frontend/my-banya/src/pages/Massages/Massages.jsx`

**Исправление:**
Удалить неиспользуемые импорты и компоненты.

---

## Проблема BUG-005: Дублирование каталога public
**Файлы:** 
- `frontend/my-banya/public/`
- `frontend/my-banya/src/public/`

**Исправление:**
Удалить `src/public/` каталог.

---

## Проблема BUG-007: Отсутствие валидации на фронтенде
**Файлы:** Все формы

**Исправление:**
Добавить базовую валидацию для форм создания/редактирования:
1. Проверка обязательных полей
2. Формат телефона
3. Валидация email
4. Проверка диапазонов чисел

---

## Проблема BUG-011: Console.log в продакшн-коде
**Файлы:** Множество файлов

**Исправление:**
Создать утилиту логирования и заменить все console.log:
```javascript
// utils/logger.js
const logger = {
  log: (...args) => process.env.NODE_ENV === 'development' && console.log(...args),
  error: (...args) => console.error(...args),
  warn: (...args) => console.warn(...args),
};
export default logger;
```

---

## Проблема BUG-015: Дублирование функции hasAccess
**Файлы:** 
- `frontend/my-banya/src/pages/Admin/Admin.jsx`
- `frontend/my-banya/src/pages/Admin/MobileSidebar.jsx`
- `frontend/my-banya/src/components/Admin/MobileBottomNav.jsx`

**Исправление:**
Создать хук `useHasAccess`:
```javascript
// hooks/useHasAccess.js
import { useSelector } from 'react-redux';

export function useHasAccess() {
  const { user } = useSelector(state => state.auth);
  
  return (permission) => {
    if (!user || !user.permissions) return false;
    return user.permissions.includes(permission) || user.is_admin;
  };
}
```

---

## План выполнения

1. Исправить BUG-002 (обработка ошибок удаления)
2. Исправить BUG-003 (race condition)
3. Исправить BUG-016 (CORS)
4. Исправить BUG-020 (N+1 queries)
5. Исправить BUG-008 (cache invalidation)
6. Исправить BUG-009 (hardcoded values)
7. Исправить BUG-010 (mobile menu permissions)
8. Исправить BUG-015 (duplicate hasAccess)
9. Исправить BUG-004 (unused components)
10. Исправить BUG-005 (duplicate public)
11. Исправить BUG-011 (console.log)
12. Исправить BUG-007 (validation)
13. Исправить BUG-017 (rate limiting)
14. Исправить BUG-022 (pagination)
15. Исправить BUG-006 (token storage)
